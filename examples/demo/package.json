{
  "name": "@fxtoolkit/example-indicator-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser demo for the indicator stdlib and runner packages.",
  "scripts": {
    "prepare:assets": "node scripts/prepare-assets.mjs",
    "dev": "npm run prepare:assets && vite",
    "build": "npm run prepare:assets && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fxtoolkit/indicator-runner": "*",
    "@fxtoolkit/indicator-stdlib": "*"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
